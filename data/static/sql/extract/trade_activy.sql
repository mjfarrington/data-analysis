SELECT
$business_date AS DATE,
$app_id AS APP_ID,
$app_name AS APP_NAME,
TRADE_ID,
TRADE_VERSION,
CUSTOMER,
PRODUCT,
PRICE,
QTY,
TRADE_DATE,
SETTLE_DATE,
BOOK
FROM TRADES
WHERE TRADE_DATE=$business_date
AND APP_ID=$app_id
AND APP_NAME=$app_name