[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integration: marks tests that require live infrastructure (Spark, gRPC, etc.)
    spark: marks tests that require a running Spark Connect server
